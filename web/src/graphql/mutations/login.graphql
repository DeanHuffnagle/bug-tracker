mutation Login($options: UserLoginInput!) {
	login(options: $options) {
		errors {
			field
			message
		}
		user {
			id
			firstName
			lastName
			email
			role
			organization {
				id
				name
				creatorId
			}
		}
	}
}
