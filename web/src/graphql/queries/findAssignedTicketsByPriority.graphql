query FindAssignedTicketsByPriority(
	$options: FindAssignedTicketsByPriorityInput!
) {
	findAssignedTicketsByPriority(options: $options) {
		id
		title
		text
		priority
		status
		type
		assignedDeveloperId
	}
}
